import { FetchProviderCode } from './fetch-provider-code';

describe('FetchProviderCode', () => {
  it('should create an instance', () => {
    expect(new FetchProviderCode()).toBeTruthy();
  });
});
